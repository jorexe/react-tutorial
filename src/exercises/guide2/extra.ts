/**
 * Exercise extra: Create a function "quadraticRoots" that receives 3 numbers (a, b, c) that represent the parameters of a quadratic function. The function should return the roots if they are real:
 * 
 * Examples:
 *   - quadraticRoots(1, -7, 10) -> "x1=2, x2=5"
 *   - quadraticRoots(1, -5, 6) -> "x1=2, x2=3" 
 *   - quadraticRoots(1, 2, 3) -> "no real roots" 
 * 
 * Note: Order of the roots does not matter
 * 
 * Helpful information: https://www.calculatorsoup.com/calculators/algebra/quadratic-formula-calculator.php
 */