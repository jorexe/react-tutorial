/**
 * Exercise 1: Write a function "printedRange" which receives two numbers (a, b) and prints (using console.log) the numbers between them, including a, but not including b
 */
export function printRange(a: number, b: number): void {
    // TODO: Implement the function
}
